# =====================================================================
# NVIDIA GPU対応 物体検出/骨格検知 統合動作環境定義
# 動作確認済み構成 (2026-05-26, Python 3.12.10, CUDA 13.2 / RTX 4070 SUPER)
# =====================================================================
# 前提: Python 3.12.x (MediaPipeがPython 3.13非対応のため固定)
# インストール方法:
#   pip install numpy==1.26.4 protobuf==4.25.6
#   pip install torch==2.12.0+cu130 torchvision==0.27.0+cu130 --extra-index-url https://download.pytorch.org/whl/cu130 --no-deps
#   pip install torchvision==0.27.0+cu130 --extra-index-url https://download.pytorch.org/whl/cu130 --no-deps
#   pip install opencv-contrib-python==4.11.0.86
#   pip install mediapipe==0.10.21
#   pip install ultralytics==8.3.100
#   pip install lapx  # track()のHungarian algorithmに必要 (Windows互換版)
#   pip install numpy==1.26.4 --force-reinstall  # ultralytics後に念のため固定
# =====================================================================
# ⚠️ 注意: pip install -r requirements.txt の一括実行では依存解決の順序が
#           保証されないため、上記の段階的インストール手順を推奨します。
#           install_env.bat を使用すると自動で正しい順序で実行されます。
# =====================================================================

# -----------------------------------------------------------------------
# 基盤バイナリ (numpy 2.x 侵入防止: MediaPipe C-API 要件)
# -----------------------------------------------------------------------
numpy==1.26.4
protobuf==4.25.6

# -----------------------------------------------------------------------
# GPU最適化 PyTorch (cu130: CUDA 13.x系 互換バイナリ)
# CUDA 12.6 の場合は cu126 に変更すること
# pip install 時に --extra-index-url https://download.pytorch.org/whl/cu130 を追加
# -----------------------------------------------------------------------
torch==2.12.0+cu130
torchvision==0.27.0+cu130

# -----------------------------------------------------------------------
# ビジョン・推論フレームワーク
# opencv-python との共存禁止 (GStreamer循環インポートバグ回避)
# -----------------------------------------------------------------------
opencv-contrib-python==4.11.0.86
mediapipe==0.10.21
ultralytics==8.3.100

# -----------------------------------------------------------------------
# YOLO track() のトラッカー依存 (Windows互換 lap 代替)
# -----------------------------------------------------------------------
lapx>=0.5.5
